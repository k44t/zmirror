#!/bin/sh
set -e

case "$1" in
  remove|upgrade|deconfigure)
    if command -v systemctl >/dev/null 2>&1; then
      systemctl stop zmirror.service || true
      systemctl stop zmirror-maintenance.timer || true
    fi
    ;;
esac

exit 0
